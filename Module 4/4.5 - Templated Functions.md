- templated variables is declared by defining it at the beginning of a class or function

`Class`
```
template <typename T>
class List {
	private:
		T data_;
}
```

`Function`
```
template <typename T>
int max(T a, T, a) {}
```

